import React from 'react';
import PropTypes from 'prop-types';
import { connect } from 'react-redux';

const PixelPerfectTabPane = () => <span>Work in progress...</span>;

export default connect(null, {})(PixelPerfectTabPane);
